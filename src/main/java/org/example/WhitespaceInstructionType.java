package org.example;

public enum WhitespaceInstructionType {
    PUSH,
    DUPLICATE,
    REFERENCE,
    SLIDE,
    SWAP,
    DISCARD,
    PLUS,
    MINUS,
    MULTIPLY,
    DIVIDE,
    MODULO,
    STORE,
    RETRIEVE,
    MARK,
    CALL,
    JUMP,
    JUMP_IF_ZERO,
    JUMP_IF_NEGATIVE,
    END,
    RETURN,
    OUTPUT_CHARACTER,
    OUTPUT_NUMBER,
    READ_CHARACTER,
    READ_NUMBER,
    EXIT
}
